# ==========================================
# TomeWeaver Core Dependencies
# ==========================================

# For handling API requests to local and cloud Large Language Models
requests>=2.31.0

# For terminal coloring (Used by the Headless API to format logs for the GUI Developer Console)
colorama>=0.4.6


# ==========================================
# TomeWeaver GUI Dependencies (Desktop App)
# ==========================================

# The modern, dark-mode UI framework (Pure Python WinForms equivalent)
customtkinter>=5.2.2

# Helper library used by CustomTkinter to automatically detect OS Dark/Light mode
darkdetect>=0.8.0

# Standard utility often required by UI frameworks for version parsing
packaging>=23.1

# For processing, cropping, and caching custom folder icons (JPG/PNG)
Pillow>=10.2.0